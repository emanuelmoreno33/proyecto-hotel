@model IEnumerable<hotel.Models.ReservationStatus>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.reservationStatusID }) |
            @Html.ActionLink("Details", "Details", new { id=item.reservationStatusID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.reservationStatusID })
        </td>
    </tr>
}

</table>
