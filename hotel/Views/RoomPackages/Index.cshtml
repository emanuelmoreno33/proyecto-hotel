@model IEnumerable<hotel.Models.RoomPackage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.namePackage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.frecuency)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RoomType.typeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.namePackage)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.frecuency)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RoomType.typeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.roomPackageID }) |
            @Html.ActionLink("Details", "Details", new { id=item.roomPackageID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.roomPackageID })
        </td>
    </tr>
}

</table>
